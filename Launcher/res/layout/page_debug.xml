<?xml version="1.0" encoding="utf-8"?>
<!-- Debug page layout with notification test buttons -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@android:color/black">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/debug_title"
        android:textSize="18sp"
        android:textColor="#808080"
        android:fontFamily="sans-serif-light"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <!-- Send immediate notification button -->
    <TextView
        android:id="@+id/btn_send_notification"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:gravity="center"
        android:text="@string/send_notification"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/debug_button_background"
        android:clickable="true"
        android:focusable="true"
        android:layout_marginBottom="16dp" />

    <!-- Toggle periodic notification button -->
    <TextView
        android:id="@+id/btn_periodic_notification"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:gravity="center"
        android:text="@string/start_periodic"
        android:textSize="18sp"
        android:textColor="@android:color/white"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/debug_button_background"
        android:clickable="true"
        android:focusable="true" />

    <!-- Status text -->
    <TextView
        android:id="@+id/periodic_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:gravity="center"
        android:textSize="14sp"
        android:textColor="#606060"
        android:fontFamily="sans-serif"
        android:visibility="gone" />

</LinearLayout>
